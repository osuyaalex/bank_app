import 'package:banking_app/parsing/bank_alert.dart';

/// Empty: the sentence reader now lives in the app, so the shipped parser is
/// being scored on its own.
BankAlert? parseFallback(String sender, String body) => null;
